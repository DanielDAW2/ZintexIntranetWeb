<form method="post" action="{{ path('t_productes_delete', {'idProd': t_producte.idProd}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_producte.idProd) }}">
    <button class="btn">Delete</button>
</form>
