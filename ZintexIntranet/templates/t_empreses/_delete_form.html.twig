<form method="post" action="{{ path('t_empreses_delete', {'id': t_emprese.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_emprese.id) }}">
    <button class="btn">Delete</button>
</form>
