<form method="post" action="{{ path('t_factura_delete', {'Id_Factura': t_factura.Id_Factura}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_factura.Id_Factura) }}">
    <button class="btn">Delete</button>
</form>
