<form method="post" action="{{ path('t_factura_prov_delete', {'idFacturaProv': t_factura_prov.idFacturaProv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_factura_prov.idFacturaProv) }}">
    <button class="btn">Delete</button>
</form>
