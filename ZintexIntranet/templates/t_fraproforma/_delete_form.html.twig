<form method="post" action="{{ path('t_fraproforma_delete', {'idFraprof': t_fraproforma.idFraprof}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_fraproforma.idFraprof) }}">
    <button class="btn">Delete</button>
</form>
