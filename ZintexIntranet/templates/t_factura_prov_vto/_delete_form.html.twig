<form method="post" action="{{ path('t_factura_prov_vto_delete', {'idFacturaProvVto': t_factura_prov_vto.idFacturaProvVto}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_factura_prov_vto.idFacturaProvVto) }}">
    <button class="btn">Delete</button>
</form>
