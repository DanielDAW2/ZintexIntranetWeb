<form method="post" action="{{ path('t_comercials_delete', {'idComercial': t_comercial.idComercial}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_comercial.idComercial) }}">
    <button class="btn">Delete</button>
</form>
